[pytest]
# Automatically run on all engines and record traces on failure
addopts = --base-url https://www.vr.fi/en --browser chromium --browser firefox --browser webkit --tracing retain-on-failure
testpaths = tests
markers =
    smoke: Quick validation tests
    regression: Full suite coverage